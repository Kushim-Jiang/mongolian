[project]
name = "mongfontbuilder"
version = "0.12.2"
description = "Python library for building standard-compatible Mongolian script fonts."
license = { text = "MIT" }
readme = "README.md"
authors = [
    { name = "Kushim Jiang", email = "kushim_jiang@outlook.com" },
    { name = "Liang Hai", email = "lianghai@gmail.com" },
]
urls.repository = "https://github.com/Kushim-Jiang/mongfontbuilder"
urls.releasenotes = "https://github.com/Kushim-Jiang/mongfontbuilder/releases"
requires-python = ">=3.10"
dependencies = [
    "cattrs>=24.1.2",
    "PyYAML>=6.0.1",
    "tptq-feacomposer>=1.11.3",
    "ufo2ft>=3.4.3",
    "ufoLib2>=0.17.1",
    "unicodedata2>=17.0.0",
]

[dependency-groups]
dev = [
    "glyphs-cli>=0.4.1;sys_platform=='darwin'",
    "glyphslib>=6.13.1",
    "pyright>=1.1.411",
    "pytest>=8.3.3",
    "ruff>=0.16.5",
    "uharfbuzz>=0.42.0",
]

[tool.uv.build-backend]
module-root = "lib"

[build-system]
requires = ["uv_build>=0.8.11,<0.9.0"]
build-backend = "uv_build"

[tool.ruff]
src = [".", "lib", "tests"] # Treat as first-party for sorting imports
line-length = 100
lint.select = [
    "E4",
    "E7",
    "E9",
    "F",
    "W",
    "I",
    "UP",
    "B",
    "C4",
    "SIM",
    "PT",
    "RUF",
]
lint.ignore = [
    "E501", # The prose of the documentation is wrapped by hand, not to a column.
    "RUF002", # The prose quotes the characters it is about: ‹ › ‘ ’ – are the point.
    "SIM118", # `ufoLib2.Font` iterates its Glyphs, not its names, so `.keys()` is meant.

]

[tool.pyright]
include = ["lib", "tests", "tools"]
extraPaths = ["lib"]

[tool.pytest.ini_options]
addopts = "lib tests --doctest-modules --doctest-report ndiff --color=yes"
